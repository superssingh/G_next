export default interface comment {
  id: string
  name: string
  email: string
  image: string
  comment: string
  date: string
}

export default interface user {
  id: string
  name: string
  email: string
  image: string
}

export default interface blog {
  id: string
  name: string
  email: string
  image: string
  comment: string
  date: string
}
